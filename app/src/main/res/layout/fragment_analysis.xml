<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <EditText
        android:id="@+id/et_chat_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="16dp"
        android:background="@color/box_grey"
        android:gravity="top|start"
        android:hint="@string/input_chat_hint"
        android:padding="16dp"
        app:layout_constraintBottom_toTopOf="@id/btn_start_analysis"
        app:layout_constraintHeight_default="spread"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_weight="1" />

    <Button
        android:id="@+id/btn_start_analysis"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/box_dark_grey"
        android:padding="16dp"
        android:text="@string/start_analysis"
        android:textColor="@color/black"
        app:layout_constraintBottom_toTopOf="@id/sv_result_container" />

    <ScrollView
        android:id="@+id/sv_result_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@color/box_grey"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHeight_default="spread"
        app:layout_constraintTop_toBottomOf="@id/btn_start_analysis"
        app:layout_constraintVertical_weight="1">

        <TextView
            android:id="@+id/tv_analysis_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:textColor="@color/black"
            android:textSize="14sp" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>